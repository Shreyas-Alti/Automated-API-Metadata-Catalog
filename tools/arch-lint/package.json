{
  "name": "@api-catalog/arch-lint",
  "version": "0.1.0",
  "private": true,
  "description": "Architecture boundary tests. Verifies that dependency-cruiser rules are correctly configured and catch violations. Run in CI after lint step.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "dependency-cruiser": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "axios": "^1.0.0",
    "openai": "^4.0.0",
    "pg": "^8.0.0"
  }
}
