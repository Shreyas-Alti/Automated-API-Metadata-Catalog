import 'reflect-metadata';
import { Module } from '@nestjs/common';

// api-service — Phase 0 stub
// Phase 2 implementation: thin HTTP layer, no business logic.
// Routes: submission, job status, review, publish, catalog browse.
// All business logic lives in the appropriate library packages.
@Module({
  imports: [],
  controllers: [],
  providers: [],
})
export class AppModule {}
