{
  "name": "@api-catalog/generator-markdown",
  "version": "0.1.0",
  "private": true,
  "description": "Pure function: ApiGraph → Markdown documentation. Never edited directly by humans.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@api-catalog/contracts": "workspace:*",
    "@api-catalog/canonical-graph": "workspace:*"
  }
}
