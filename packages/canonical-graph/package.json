{
  "name": "@api-catalog/canonical-graph",
  "version": "0.1.0",
  "private": true,
  "description": "Domain model (Repository → API → Endpoint → Schema → Auth → Version). The sole source of truth.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@api-catalog/contracts": "workspace:*"
  }
}
