{
  "name": "@api-catalog/repository-loader",
  "version": "0.1.0",
  "private": true,
  "description": "Clone and prepare a git repository for extraction. Validates that the URL points to an allowed git host before cloning.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@api-catalog/contracts": "workspace:*"
  }
}
