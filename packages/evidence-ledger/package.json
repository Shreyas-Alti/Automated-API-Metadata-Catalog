{
  "name": "@api-catalog/evidence-ledger",
  "version": "0.1.0",
  "private": true,
  "description": "Append-only EvidenceRecord store. No update/delete. Denormalized per-endpoint summary recomputed on each append.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@api-catalog/contracts": "workspace:*"
  }
}
