{
  "name": "@api-catalog/parser-express",
  "version": "1.0.0",
  "private": true,
  "description": "Express.js parser — versioned, capability-declared, golden-repo tested. Phase 1 implementation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@api-catalog/contracts": "workspace:*",
    "@api-catalog/parser-registry": "workspace:*"
  }
}
