{
  "name": "@api-catalog/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared type contracts — ExtractionResult, ExtractionRun, EvidenceRecord, canonical graph entities, quality score shape",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {}
}
